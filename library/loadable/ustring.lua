---@meta ustring
---
---The pure-Lua backend to the Ustring library. In all cases the Ustring library ([mw.ustring](lua://mw.ustring)) should be used instead, as that replaces many of the slower and more memory-intensive operations with callbacks into PHP code.

return mw.ustring
